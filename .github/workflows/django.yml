name: Django CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    env:
      PIPENV_NO_INHERIT: 1
      PIPENV_IGNORES_VIRTUALENVS: 1
    runs-on: ubuntu-latest
    
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.6, 3.7, 3.8]

    services:
      postgres:
        image: 12.5
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports: ["5432:5432"]

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Copying configurations
          run: |
            cp contrib/env-sample .env
      - name: Preparing Server
        run: |
          sudo apt-get install -y build-essential libssl-dev zlib1g-dev libbz2-dev libreadline-dev libsqlite3-dev wget curl llvm gettext libncurses5-dev tk-dev tcl-dev blt-dev libgdbm-dev git python-dev python3-dev aria2 vim libnss3-tools python3-venv liblzma-dev
      - name: Install pipenv
        run: |
          python -m pip install --upgrade pip
          pip install pipenv
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pipenv install --dev
      - name: Run Django
        run: |
          pipenv run python manage.py test
      - name: Validation PEP8
        run: |
          pipenv run flake8 .
      - name: Run Tests
        run: |
          pipenv run pytest --cov=api --doctest-modules --junitxml=junit/test-results.xml --cov=com --cov-report=xml --cov-report=html
